import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.twostroke.bikemodguide',
  appName: '2-Stroke-Bike-Mod-Guide',
  webDir: 'dist'
};

export default config;
